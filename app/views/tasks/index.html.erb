<h1>My Tasks</h1>

<ul>
  <% @tasks.each do |task| %>
  <input type="checkbox" id="task" name="task">
  <label for="checkbox"><%= link_to task.title, task_path(task.id) %></label><br>
  <% end %>
</ul>

<p><%= link_to "+ Add a new tast", new_task_path %></p>
